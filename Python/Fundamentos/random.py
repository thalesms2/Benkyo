import random
numero = random.randint(0, 10) # Primeiro numero até o ultimo
print(numero)
# random.seed(1) // Força dar o mesmo numero sempre
lista = [6,45,2,9,12]
numeroLista = random.choice(lista)
print(numeroLista)