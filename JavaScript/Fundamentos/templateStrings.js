const name = 'Rebeca'
const concatenacao = 'Olá ' + name + '!'

console.log(concatenacao)

const template = `
    Olá
    ${name}!` // aberto com acento à <<
    console.log(template)

    //expressoes
    console.log(`1 + 1 = ${1 + 1}`)

    const up = texto => texto.toUpperCase()
    console.log(`Ei... ${up('cuidado')}!`)