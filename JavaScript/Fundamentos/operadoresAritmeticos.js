const [a, b, c, d] = [1, 3, 5, 10]

const soma = a + b + c + d
const subtracao = a - b
const multiplicacao = a * d
const divisao = c / d
const modulo = a % b

console.log(soma, subtracao, multiplicacao, divisao, modulo)
console.log(soma, subtracao, multiplicacao, -divisao, modulo)